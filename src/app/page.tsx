import { HeroSection } from "@/components/hero";

export default function Home() {
  return (
    <main className="h-[100dvh] overflow-hidden md:overflow-auto flex items-center justify-center">
      <HeroSection />
    </main>
  );
}
