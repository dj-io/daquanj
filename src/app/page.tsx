import { HeroSection } from "@/components/hero";

export default function Home() {
  return (
    <main className="h-[100svh] overflow-hidden overscroll-none flex items-center justify-center">
      <HeroSection />
    </main>
  );
}
